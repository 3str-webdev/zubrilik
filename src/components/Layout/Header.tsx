import { Typography } from "@mui/material";
import React from "react";

const Header = () => {
  return (
    <header className="react-header">
      <Typography variant="h3" className="title">Зубрилик</Typography>
    </header>
  );
};

export default Header;
